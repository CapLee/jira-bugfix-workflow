# 02 · 使用方法

> 全文命令以 `python3 scripts/jira.py` 为例；Windows 下 `python scripts/jira.py` 同理。
> 每个子命令都有 `--help`。

## 1. 命令参考

### search —— 拉 bug 清单（工作流第一步）

```bash
python3 scripts/jira.py search --jql 'JQL' [--max 100] [--format table|json|md]
```

- `--jql`：原始 JQL，必填。示例：
  - 全部未解决且指派给我（最常用）：`resolution = Unresolved AND assignee in (currentUser()) order by updated DESC`
  - 指定项目未开始：`project = X AND status = 未开始 AND assignee in (currentUser()) order by updated DESC`
  - 处理中：`project = X AND status = Working AND resolution = Unresolved …`
- `--format md`：输出 markdown 表格，方便贴给 AI/文档；`json` 输出原始响应

### issue —— 拉单详情 + 全部评论（AI 读单输入）

```bash
python3 scripts/jira.py issue STRUCTURING-25 [--save D:/tmp/s25.md]
```

输出：状态/优先级/类型/报告人/经办人/时间 + 描述 + 评论时间线。`--save` 存成 markdown 文件供 AI 逐段阅读。

### transitions —— 查看某单当前可用的状态流转

```bash
python3 scripts/jira.py transitions STRUCTURING-25
# 41	接受	-> Working (category=indeterminate)
# 11	解决	-> ST Check (category=indeterminate)
```

### start —— 「接受」（未开始 → Working）

```bash
python3 scripts/jira.py start STRUCTURING-25
# 已流转: 接受 -> Working；读回: STRUCTURING-25 -> Working (resolution=-)
```

按名称匹配「接受」类流转（不硬编码 id）；名称歧义时用 `--transition-id 41` 强制指定。

### resolve —— 流转「解决」+ 填自定义字段 + 转交测试（核心写操作）

```bash
python3 scripts/jira.py resolve STRUCTURING-25 \
  --impact "影响范围（自由文本，textarea）" \
  --cause "发生原因（下拉选项，自动校验）" \
  --solution "解决方法（下拉选项，自动校验）" \
  --prevention "预防措施（下拉选项，自动校验）" \
  [--field "修复的版本=V1.0.0_1"]...   # 附加屏幕字段，可多次
  [--assign "张三"]                    # 解决后显式转交某人
  [--transition-id 11]                 # 流转名歧义时强制指定
```

执行过程（全部自动，无硬编码）：
1. 读取该单可用流转，按名称含「解决」选中（目标可能是中间态如 ST Check，不是最终态）
2. 展开流转屏幕字段（`transitions?expand=transitions.fields`），按中文标签匹配四个标准字段；**字段 id 每次运行时发现**
3. select 字段自动打印可选值词表，并对你传入的值做校验：不在词表 → **请求发出前中止**并列出可选项（避免盲发 400）；标点差异（`、` vs `/`）自动容错
4. 值按字段类型自动包装：选项 → option id；版本 → 数组；用户 → name；文本框 → 原值
5. 必填检查通过后发流转，**自动读回状态验证**

> ⚠️ 词表、字段标签、状态名**因项目而异**——工具每次运行都现场发现，换项目不用改任何代码。**别硬背词表**，以命令输出为准。

## 2. 六步数据处理工作流（人 / AI 通用）

| 步骤 | 动作 | 命令或自然语言 |
|---|---|---|
| ① 拉清单 | 按 JQL 拉全量，编号呈现 | `search --jql '…'` / 用户：「拉一下我未解决的 bug」 |
| ② 圈单+读单 | 用户圈定（建议每轮 ≤5 张），逐张拉详情+评论 | `issue KEY` / 用户：「1、3、7 分析下根因」 |
| ③ 定位修复 | AI 读单定位代码、给「根因+改法+文件」经确认后修改+自验证；涉及表结构交付 DDL/DML；改动留工作区不提交 | 用户：「这几张都改」 |
| ④ 接受 | 修复验证通过 → 点「接受」 | `start KEY` / 用户：「接受吧」 |
| ⑤ 解决 | 用户验收后 → resolve，填影响范围/原因/方法/预防 + 版本，必要时转交 | `resolve KEY …` / 用户：「这张修好了，流转吧」 |
| ⑥ 收尾汇报 | 单号+根因+改动文件+验证+状态；代码提交由开发者自己做 | — |

**安全约定（写操作双保险）**：start / resolve 永远等用户明说一句再执行；每次流转后读回验证；resolve 的具体填值（影响范围写法、原因/方法词表选择）在批量场景先给用户过目或按用户既定默认执行。

**批量场景**（如「把 Working 的全部解决」）：逐张拉详情 → 按 bug 性质归类选词表值（UI 缺失→需求理解偏差/调整UI样式；状态重置/权限遗漏→逻辑边界遗漏/增加边界处理；行为逻辑错→代码逻辑错误/修改业务逻辑）→ 逐张执行并各自读回 → 最后用原 JQL 复跑确认清零。

## 3. 自然语言触发对照（AI 助手接入用）

| 用户说（大意） | AI 做 |
|---|---|
| 「拉一下我的 bug / 丢 JQL 链接」 | search 拉清单编号呈现，等圈单 |
| 「看下 STRUCTURING-25 什么内容」 | issue KEY 拉详情+评论 |
| 「1、3、7 先分析下根因」 | 逐张详情→代码定位→列根因+改法（**不改码不流转**） |
| 「这几张都改 / 按你分析的改」 | 确认后逐张改码+自验证（不 commit） |
| 「接受吧 / 开始修吧」 | start KEY |
| 「这张修好了，解决/流转吧」 | resolve：影响范围按 bug 写、原因/方法按词表选、预防措施默认「无需额外措施」、修复版本默认团队当前版本 → 读回 |
| 「版本 X / 转给张三 / 预防措施填 Y」 | 覆盖对应参数 |
| 「把 Working 的批量解决」 | 逐张列拟填内容→确认后批量执行 |
| 「我只要 resolve，不用改码」 | 跳过改码直接流转 |
| 「XX 单现在什么状态」 | issue?fields=status + transitions |

## 4. 完整实测示例

以下为真实项目（项目代号 STRUCTURING，JIRA Server 8.13，中文界面）的运行实录，字段 id 与词表为**该项目现场值**，仅作形态示范——你的项目会在运行时自动打印自己的词表。

### 拉清单

```bash
$ python3 jira.py search --jql 'project = STRUCTURING AND resolution = Unresolved AND assignee in (currentUser()) order by updated DESC' --format md
共 21 张（已取 21）
| KEY | 类型 | 状态 | 优先级 | 更新 | 摘要 |
|---|---|---|---|---|---|
| STRUCTURING-25 | ST-BUG | 未开始 | Medium-一般 | 2026-09-08 | 【我的空间列表】置顶按钮常显… |
…
```

### resolve 实录（STRUCTURING-3）

```bash
$ python3 jira.py resolve STRUCTURING-3 \
    --impact "系统管理-样式模板管理-列表页筛选区（查询/重置按钮展示与触发），不涉及接口与数据变更" \
    --cause "需求理解偏差" --solution "调整UI、样式" --prevention "无需额外措施" \
    --field "修复的版本=V1.0.0_1"
字段映射: 影响范围 -> customfield_13209
字段映射: 发生原因 -> customfield_15817
  可选值: 需求文档遗漏 / 需求理解偏差 / 逻辑边界遗漏 / 数据状态异常 / 接口调用异常 / 兼容性问题 / 代码逻辑错误 / 配置错误 / 第三方依赖问题 / 性能/并发问题 / 回归引入 / 其他
字段映射: 解决方法 -> customfield_15818
  可选值: 完善需求文档 / 修改业务逻辑 / 增加边界处理 / 修复接口调用 / 调整UI/样式 / 更新配置文件 / 回滚代码 / 升级/替换依赖 / 优化SQL/索引 / 增加异常捕获 / 数据修复 / 其他
字段映射: 预防措施 -> customfield_15819
  可选值: 完善代码审查Checklist / 优化需求文档 / 增加单元测试 / 引入静态代码扫描规则 / 建立/更新开发规范 / 组织技术分享 / 加强自测流程 / 优化接口文档 / 无需额外措施 / 其他
已流转 STRUCTURING-3: 解决 -> ST Check；读回: STRUCTURING-3 -> ST Check (resolution=-)
```

> 注意实录里输入的 `调整UI、样式`（顿号）被自动容错匹配到选项 `调整UI/样式`（斜杠）——标点差异不影响。

### 选项校验实录（错误值被拦截）

```bash
$ python3 jira.py resolve X-1 --cause "不存在的选项XYZ" …
[错误] 字段 customfield_15817(发生原因) 值「不存在的选项XYZ」不在可选值内，可选项: 需求文档遗漏、需求理解偏差、…、其他
# 单子状态未被改动（在 POST 前中止）
```

### 批量处理实录

15 张 Working 单逐张 resolve：影响范围按各自 bug 撰写、原因/方法按性质归类、预防措施统一「无需额外措施」、版本统一 V1.0.0_1 → 15/15 成功读回 ST Check → 原 JQL 复跑 `共 0 张` 清零确认。
